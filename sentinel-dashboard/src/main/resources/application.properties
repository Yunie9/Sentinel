#spring settings
server.servlet.encoding.force=true
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.port=8848

#cookie name setting
server.servlet.session.cookie.name=sentinel_dashboard_cookie

#logging settings
logging.level.org.springframework.web=INFO
logging.file.name=${user.home}/logs/csp/sentinel-dashboard.log
logging.pattern.file= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
#logging.pattern.console= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

#auth settings
auth.filter.exclude-urls=/,/auth/login,/auth/logout,/registry/machine,/version
auth.filter.exclude-url-suffixes=htm,html,js,css,map,ico,ttf,woff,png
# If auth.enabled=false, Sentinel console disable login
auth.username=sentinel
auth.password=sentinel

# Inject the dashboard version. It's required to enable
# filtering in pom.xml for this resource file.
sentinel.dashboard.version=@project.version@

apollo.meta=http://localhost:18080
apollo.token=b4aa019c5a9f122bd18dde1fd2c99015038fea42f46932b9d956a71c487cb1e3
apollo.connectTimeout=2000
apollo.readTimeout=2000
apollo.appId=sentinel
apollo.env=DEV
apollo.clusterName=default
apollo.namespaceName=application
apollo.emergencyPublish=true
apollo.releaseComment=Modify or add configurations
apollo.releasedBy=Sentinel Dashboard
apollo.releaseTitle=Modify or add configurations
